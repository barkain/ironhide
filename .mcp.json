{
  "mcpServers": {
    "analytics": {
      "command": "bun",
      "args": ["run", "apps/server/src/index.ts", "--mcp", "--with-http"],
      "env": {
        "CLAUDE_SESSIONS_PATH": "${HOME}/.claude/projects"
      }
    }
  }
}
